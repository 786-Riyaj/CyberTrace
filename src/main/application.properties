spring.application.name=CyberTrace

# --- DATABASE CONFIG (H2 - In Memory) ---
# Ye settings bina kisi external MySQL ke chalengi
spring.datasource.url=jdbc:h2:mem:cyberdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# --- JPA / HIBERNATE SETTINGS ---
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.h2.console.enabled=true

# --- SERVER SETTINGS ---
# Render ke liye ye line bahut important hai
server.port=${PORT:8080}

# --- MAIL SETTINGS ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
